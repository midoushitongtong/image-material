// category list item
export type CategoryListItem = {
  id: string;
  name: string;
};

// hot category list item
export type HotCategoryListItem = {
  id: string;
  photo: string;
  title: string;
};
